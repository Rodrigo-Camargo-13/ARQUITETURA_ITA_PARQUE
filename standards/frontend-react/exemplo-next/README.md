# Exemplo — Next.js (TypeScript)

## Objetivo
Projeto mínimo com página de health e fetch a uma API.

## Comandos
```bash
npm install
npm run dev
npm run build && npm run start

Padrões
TypeScript strict; ESLint + a11y

React Query para dados remotos

Web Vitals e rastreamento de erros