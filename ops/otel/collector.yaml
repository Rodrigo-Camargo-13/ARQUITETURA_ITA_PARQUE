receivers:
  otlp:
    protocols: { http: {}, grpc: {} }
exporters:
  otlp:
    endpoint: jaeger-collector:4317
  logging: { }
processors:
  batch: {}
service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlp, logging]
